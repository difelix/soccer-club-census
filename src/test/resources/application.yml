server:
  port: 8080
  servlet:
    context-path: /soccer-club-census

spring:
  datasource:
    url: jdbc:h2:mem:soccer-club-census-db;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: difelix
    password: 123
    driverClassName: org.h2.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format-sql: true